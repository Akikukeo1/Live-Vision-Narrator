name: CI

on: [push, pull_request]

jobs:
  runner:
    name: ${{ matrix.language }} ${{ matrix.task == 'lint' && 'LintCheck' || matrix.task == 'fmt' && 'FmtCheck' || matrix.task == 'build' && 'BuildCheck' || 'Test' }} (${{ matrix.os == 'windows-latest' && 'Windows' || 'Ubuntu' }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        language: [go, rust]
        os: [ubuntu-latest, windows-latest]
        task: [build, test]
        go-version: ['1.26']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        if: matrix.language == 'go'
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          check-latest: false
          cache: true
          cache-dependency-path: "**/go.sum"

      - name: Set up Rust
        if: matrix.language == 'rust'
        uses: dtolnay/rust-toolchain@stable

      # Rust キャッシュ（Rust のみ）
      - name: Cache Rust
        if: matrix.language == 'rust'
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "src-rust -> target"
          prefix-key: "v3-rust-${{ runner.os }}"
          cache-targets: true

      - name: Run Task
        shell: bash
        run: |
          if [ "${{ matrix.language }}" == "go" ]; then
            if [ "${{ matrix.task }}" == "build" ]; then
              echo "--- Go Build on ${{ matrix.os }} ---"
              (cd src-go && go build ./...)
            else
              echo "--- Go Test on ${{ matrix.os }} ---"
              (cd src-go && go test ./...)
            fi
          else
            if [ "${{ matrix.task }}" == "build" ]; then
              echo "--- Rust Build on ${{ matrix.os }} ---"
              cargo build --manifest-path src-rust/Cargo.toml
            else
              echo "--- Rust Test on ${{ matrix.os }} ---"
              cargo test --manifest-path src-rust/Cargo.toml
            fi
          fi

  lint-fmt:
    name: ${{ matrix.language }} ${{ matrix.task == 'lint' && 'LintCheck' || matrix.task == 'fmt' && 'FmtCheck' }} (Ubuntu)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        language: [go, rust]
        task: [lint, fmt]
        go-version: ['1.26']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        if: matrix.language == 'go'
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          check-latest: false
          cache: true
          cache-dependency-path: "**/go.sum"

      - name: Cache Go build and module cache
        if: matrix.language == 'go'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: go-cache-${{ runner.os }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            go-cache-${{ runner.os }}-

      - name: Install Go format tool
        if: matrix.language == 'go'
        run: |
          go install mvdan.cc/gofumpt@latest

      - name: Restore golangci-lint binary cache
        if: matrix.language == 'go' && matrix.task == 'lint'
        uses: actions/cache@v4
        with:
          path: |
            ${{ github.workspace }}/.gobin
            $HOME/go/bin/golangci-lint
          key: golangci-lint-${{ runner.os }}-v1.64.8-go-${{ matrix.go-version }}
          restore-keys: |
            golangci-lint-${{ runner.os }}-

      - name: Install golangci-lint via go install
        if: matrix.language == 'go' && matrix.task == 'lint'
        shell: bash
        run: |
          if [ ! -x "$HOME/go/bin/golangci-lint" ]; then
            go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.64.8
          fi
          echo "$HOME/go/bin" >> $GITHUB_PATH

      - name: Run golangci-lint
        if: matrix.language == 'go' && matrix.task == 'lint'
        shell: bash
        run: |
          golangci-lint run --out-format=colored-line-number --config .golangci.yml ./src-go/...

      - name: Save golangci-lint binary cache
        if: matrix.language == 'go' && matrix.task == 'lint'
        uses: actions/cache@v4
        with:
          path: |
            $HOME/go/bin/golangci-lint
          key: golangci-lint-${{ runner.os }}-v1.64.8-go-${{ matrix.go-version }}

      - name: Run gofmt check
        if: matrix.language == 'go' && matrix.task == 'fmt'
        shell: bash
        run: |
          gofumpt -l -e src-go

      - name: Set up Rust
        if: matrix.language == 'rust'
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        if: matrix.language == 'rust'
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "src-rust -> target"
          prefix-key: "v3-rust-${{ runner.os }}"
          cache-targets: true

      - name: Run cargo clippy
        if: matrix.language == 'rust' && matrix.task == 'lint'
        shell: bash
        run: |
          cargo clippy --manifest-path src-rust/Cargo.toml -- -D warnings

      - name: Run cargo fmt check
        if: matrix.language == 'rust' && matrix.task == 'fmt'
        shell: bash
        run: |
          cargo fmt --manifest-path src-rust/Cargo.toml -- --check
